# タプルの作成
a = (1, 2, 3, 4, 5)
b = (1, )  # 要素が一つの場合はコンマが必要
c = 1, 3, 5  # 括弧を省略して書いた場合もタプルになる

print(a)
print(b)
print(c)


# タプルは要素の追加・変更・削除ができない
# そのため，辞書のキーや集合の要素など、要素が変化すると不都合なものに使える（辞書や集合については後出）
